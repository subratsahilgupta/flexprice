- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
[- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
1- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
2- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
5- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
7- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
]- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
2- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
0- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
2- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
6- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
0- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
9- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
1- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
0- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
1- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
2- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
5- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
7- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
·- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
[- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
]- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
H- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
j- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
z- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
K- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
z- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
z- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
j- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
<- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
>- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
→- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
→- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
→- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
z- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
J- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
z- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
→- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
j- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
3- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
→- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
→- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
H- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
H- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
≤- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
V- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
V- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
H- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
X- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
Q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
U- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
M- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
Q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
3- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
j- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
1- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
2- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
3- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
P- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
V- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
O- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
4- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
j- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
j- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
E- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
j- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
"- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
"- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
q- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
W- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
G- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
C- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
Σ- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
'- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
Σ- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
F- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
N- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
L- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
Σ- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
=- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
−- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
z- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
R- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
I- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
|- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
*- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
:- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
—- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
#- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
b- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
B- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
T- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
S- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
(- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
-- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
,- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
D- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
)- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
2- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
;- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
/- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
x- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
d- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
+- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
f- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
A- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
y- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
k- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
Σ- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
_- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
u- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
`- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
n- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
p- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
v- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
w- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
z- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
e- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
r- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
i- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
m- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
a- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
t- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
c- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
h- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
 - **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
l- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
o- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
g- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
s- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.
.- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

- **Day-grained bounds, in the subscription's timezone.** `period_start`,
  `period_end` and `day` are DATE columns, so every write and every lookup goes
  through `periodDays` / `dayOf` — and both take a `*time.Location`, because
  `buildUsageCurve` splits usage into **local** days. Truncating in UTC instead
  puts the bound a day off for any non-UTC subscription, and the flip then
  misses a row the curve wrote. `inclusiveLastDay` must also agree with that
  walk: the last day is the one *before* the exclusive end's own local date,
  because that date opens the next period. A period shorter than a day is
  clamped to its own date rather than inverting.

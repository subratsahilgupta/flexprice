// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flexprice/flexprice/ent/predicate"
	"github.com/flexprice/flexprice/ent/revenuefact"
)

// RevenueFactQuery is the builder for querying RevenueFact entities.
type RevenueFactQuery struct {
	config
	ctx        *QueryContext
	order      []revenuefact.OrderOption
	inters     []Interceptor
	predicates []predicate.RevenueFact
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the RevenueFactQuery builder.
func (rfq *RevenueFactQuery) Where(ps ...predicate.RevenueFact) *RevenueFactQuery {
	rfq.predicates = append(rfq.predicates, ps...)
	return rfq
}

// Limit the number of records to be returned by this query.
func (rfq *RevenueFactQuery) Limit(limit int) *RevenueFactQuery {
	rfq.ctx.Limit = &limit
	return rfq
}

// Offset to start from.
func (rfq *RevenueFactQuery) Offset(offset int) *RevenueFactQuery {
	rfq.ctx.Offset = &offset
	return rfq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (rfq *RevenueFactQuery) Unique(unique bool) *RevenueFactQuery {
	rfq.ctx.Unique = &unique
	return rfq
}

// Order specifies how the records should be ordered.
func (rfq *RevenueFactQuery) Order(o ...revenuefact.OrderOption) *RevenueFactQuery {
	rfq.order = append(rfq.order, o...)
	return rfq
}

// First returns the first RevenueFact entity from the query.
// Returns a *NotFoundError when no RevenueFact was found.
func (rfq *RevenueFactQuery) First(ctx context.Context) (*RevenueFact, error) {
	nodes, err := rfq.Limit(1).All(setContextOp(ctx, rfq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{revenuefact.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (rfq *RevenueFactQuery) FirstX(ctx context.Context) *RevenueFact {
	node, err := rfq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first RevenueFact ID from the query.
// Returns a *NotFoundError when no RevenueFact ID was found.
func (rfq *RevenueFactQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rfq.Limit(1).IDs(setContextOp(ctx, rfq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{revenuefact.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (rfq *RevenueFactQuery) FirstIDX(ctx context.Context) string {
	id, err := rfq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single RevenueFact entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one RevenueFact entity is found.
// Returns a *NotFoundError when no RevenueFact entities are found.
func (rfq *RevenueFactQuery) Only(ctx context.Context) (*RevenueFact, error) {
	nodes, err := rfq.Limit(2).All(setContextOp(ctx, rfq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{revenuefact.Label}
	default:
		return nil, &NotSingularError{revenuefact.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (rfq *RevenueFactQuery) OnlyX(ctx context.Context) *RevenueFact {
	node, err := rfq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only RevenueFact ID in the query.
// Returns a *NotSingularError when more than one RevenueFact ID is found.
// Returns a *NotFoundError when no entities are found.
func (rfq *RevenueFactQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = rfq.Limit(2).IDs(setContextOp(ctx, rfq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{revenuefact.Label}
	default:
		err = &NotSingularError{revenuefact.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (rfq *RevenueFactQuery) OnlyIDX(ctx context.Context) string {
	id, err := rfq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of RevenueFacts.
func (rfq *RevenueFactQuery) All(ctx context.Context) ([]*RevenueFact, error) {
	ctx = setContextOp(ctx, rfq.ctx, ent.OpQueryAll)
	if err := rfq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*RevenueFact, *RevenueFactQuery]()
	return withInterceptors[[]*RevenueFact](ctx, rfq, qr, rfq.inters)
}

// AllX is like All, but panics if an error occurs.
func (rfq *RevenueFactQuery) AllX(ctx context.Context) []*RevenueFact {
	nodes, err := rfq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of RevenueFact IDs.
func (rfq *RevenueFactQuery) IDs(ctx context.Context) (ids []string, err error) {
	if rfq.ctx.Unique == nil && rfq.path != nil {
		rfq.Unique(true)
	}
	ctx = setContextOp(ctx, rfq.ctx, ent.OpQueryIDs)
	if err = rfq.Select(revenuefact.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (rfq *RevenueFactQuery) IDsX(ctx context.Context) []string {
	ids, err := rfq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (rfq *RevenueFactQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, rfq.ctx, ent.OpQueryCount)
	if err := rfq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, rfq, querierCount[*RevenueFactQuery](), rfq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (rfq *RevenueFactQuery) CountX(ctx context.Context) int {
	count, err := rfq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (rfq *RevenueFactQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, rfq.ctx, ent.OpQueryExist)
	switch _, err := rfq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (rfq *RevenueFactQuery) ExistX(ctx context.Context) bool {
	exist, err := rfq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the RevenueFactQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (rfq *RevenueFactQuery) Clone() *RevenueFactQuery {
	if rfq == nil {
		return nil
	}
	return &RevenueFactQuery{
		config:     rfq.config,
		ctx:        rfq.ctx.Clone(),
		order:      append([]revenuefact.OrderOption{}, rfq.order...),
		inters:     append([]Interceptor{}, rfq.inters...),
		predicates: append([]predicate.RevenueFact{}, rfq.predicates...),
		// clone intermediate query.
		sql:  rfq.sql.Clone(),
		path: rfq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.RevenueFact.Query().
//		GroupBy(revenuefact.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (rfq *RevenueFactQuery) GroupBy(field string, fields ...string) *RevenueFactGroupBy {
	rfq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &RevenueFactGroupBy{build: rfq}
	grbuild.flds = &rfq.ctx.Fields
	grbuild.label = revenuefact.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.RevenueFact.Query().
//		Select(revenuefact.FieldTenantID).
//		Scan(ctx, &v)
func (rfq *RevenueFactQuery) Select(fields ...string) *RevenueFactSelect {
	rfq.ctx.Fields = append(rfq.ctx.Fields, fields...)
	sbuild := &RevenueFactSelect{RevenueFactQuery: rfq}
	sbuild.label = revenuefact.Label
	sbuild.flds, sbuild.scan = &rfq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a RevenueFactSelect configured with the given aggregations.
func (rfq *RevenueFactQuery) Aggregate(fns ...AggregateFunc) *RevenueFactSelect {
	return rfq.Select().Aggregate(fns...)
}

func (rfq *RevenueFactQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range rfq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, rfq); err != nil {
				return err
			}
		}
	}
	for _, f := range rfq.ctx.Fields {
		if !revenuefact.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if rfq.path != nil {
		prev, err := rfq.path(ctx)
		if err != nil {
			return err
		}
		rfq.sql = prev
	}
	return nil
}

func (rfq *RevenueFactQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*RevenueFact, error) {
	var (
		nodes = []*RevenueFact{}
		_spec = rfq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*RevenueFact).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &RevenueFact{config: rfq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, rfq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (rfq *RevenueFactQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := rfq.querySpec()
	_spec.Node.Columns = rfq.ctx.Fields
	if len(rfq.ctx.Fields) > 0 {
		_spec.Unique = rfq.ctx.Unique != nil && *rfq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, rfq.driver, _spec)
}

func (rfq *RevenueFactQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(revenuefact.Table, revenuefact.Columns, sqlgraph.NewFieldSpec(revenuefact.FieldID, field.TypeString))
	_spec.From = rfq.sql
	if unique := rfq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if rfq.path != nil {
		_spec.Unique = true
	}
	if fields := rfq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, revenuefact.FieldID)
		for i := range fields {
			if fields[i] != revenuefact.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := rfq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := rfq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := rfq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := rfq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (rfq *RevenueFactQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(rfq.driver.Dialect())
	t1 := builder.Table(revenuefact.Table)
	columns := rfq.ctx.Fields
	if len(columns) == 0 {
		columns = revenuefact.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if rfq.sql != nil {
		selector = rfq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if rfq.ctx.Unique != nil && *rfq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range rfq.predicates {
		p(selector)
	}
	for _, p := range rfq.order {
		p(selector)
	}
	if offset := rfq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := rfq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// RevenueFactGroupBy is the group-by builder for RevenueFact entities.
type RevenueFactGroupBy struct {
	selector
	build *RevenueFactQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (rfgb *RevenueFactGroupBy) Aggregate(fns ...AggregateFunc) *RevenueFactGroupBy {
	rfgb.fns = append(rfgb.fns, fns...)
	return rfgb
}

// Scan applies the selector query and scans the result into the given value.
func (rfgb *RevenueFactGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rfgb.build.ctx, ent.OpQueryGroupBy)
	if err := rfgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RevenueFactQuery, *RevenueFactGroupBy](ctx, rfgb.build, rfgb, rfgb.build.inters, v)
}

func (rfgb *RevenueFactGroupBy) sqlScan(ctx context.Context, root *RevenueFactQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(rfgb.fns))
	for _, fn := range rfgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*rfgb.flds)+len(rfgb.fns))
		for _, f := range *rfgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*rfgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rfgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// RevenueFactSelect is the builder for selecting fields of RevenueFact entities.
type RevenueFactSelect struct {
	*RevenueFactQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (rfs *RevenueFactSelect) Aggregate(fns ...AggregateFunc) *RevenueFactSelect {
	rfs.fns = append(rfs.fns, fns...)
	return rfs
}

// Scan applies the selector query and scans the result into the given value.
func (rfs *RevenueFactSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, rfs.ctx, ent.OpQuerySelect)
	if err := rfs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*RevenueFactQuery, *RevenueFactSelect](ctx, rfs.RevenueFactQuery, rfs, rfs.inters, v)
}

func (rfs *RevenueFactSelect) sqlScan(ctx context.Context, root *RevenueFactQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(rfs.fns))
	for _, fn := range rfs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*rfs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := rfs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flexprice/flexprice/ent/entitlementgrant"
	"github.com/flexprice/flexprice/ent/predicate"
)

// EntitlementGrantQuery is the builder for querying EntitlementGrant entities.
type EntitlementGrantQuery struct {
	config
	ctx        *QueryContext
	order      []entitlementgrant.OrderOption
	inters     []Interceptor
	predicates []predicate.EntitlementGrant
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the EntitlementGrantQuery builder.
func (egq *EntitlementGrantQuery) Where(ps ...predicate.EntitlementGrant) *EntitlementGrantQuery {
	egq.predicates = append(egq.predicates, ps...)
	return egq
}

// Limit the number of records to be returned by this query.
func (egq *EntitlementGrantQuery) Limit(limit int) *EntitlementGrantQuery {
	egq.ctx.Limit = &limit
	return egq
}

// Offset to start from.
func (egq *EntitlementGrantQuery) Offset(offset int) *EntitlementGrantQuery {
	egq.ctx.Offset = &offset
	return egq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (egq *EntitlementGrantQuery) Unique(unique bool) *EntitlementGrantQuery {
	egq.ctx.Unique = &unique
	return egq
}

// Order specifies how the records should be ordered.
func (egq *EntitlementGrantQuery) Order(o ...entitlementgrant.OrderOption) *EntitlementGrantQuery {
	egq.order = append(egq.order, o...)
	return egq
}

// First returns the first EntitlementGrant entity from the query.
// Returns a *NotFoundError when no EntitlementGrant was found.
func (egq *EntitlementGrantQuery) First(ctx context.Context) (*EntitlementGrant, error) {
	nodes, err := egq.Limit(1).All(setContextOp(ctx, egq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{entitlementgrant.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (egq *EntitlementGrantQuery) FirstX(ctx context.Context) *EntitlementGrant {
	node, err := egq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first EntitlementGrant ID from the query.
// Returns a *NotFoundError when no EntitlementGrant ID was found.
func (egq *EntitlementGrantQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = egq.Limit(1).IDs(setContextOp(ctx, egq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{entitlementgrant.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (egq *EntitlementGrantQuery) FirstIDX(ctx context.Context) string {
	id, err := egq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single EntitlementGrant entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one EntitlementGrant entity is found.
// Returns a *NotFoundError when no EntitlementGrant entities are found.
func (egq *EntitlementGrantQuery) Only(ctx context.Context) (*EntitlementGrant, error) {
	nodes, err := egq.Limit(2).All(setContextOp(ctx, egq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{entitlementgrant.Label}
	default:
		return nil, &NotSingularError{entitlementgrant.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (egq *EntitlementGrantQuery) OnlyX(ctx context.Context) *EntitlementGrant {
	node, err := egq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only EntitlementGrant ID in the query.
// Returns a *NotSingularError when more than one EntitlementGrant ID is found.
// Returns a *NotFoundError when no entities are found.
func (egq *EntitlementGrantQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = egq.Limit(2).IDs(setContextOp(ctx, egq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{entitlementgrant.Label}
	default:
		err = &NotSingularError{entitlementgrant.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (egq *EntitlementGrantQuery) OnlyIDX(ctx context.Context) string {
	id, err := egq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of EntitlementGrants.
func (egq *EntitlementGrantQuery) All(ctx context.Context) ([]*EntitlementGrant, error) {
	ctx = setContextOp(ctx, egq.ctx, ent.OpQueryAll)
	if err := egq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*EntitlementGrant, *EntitlementGrantQuery]()
	return withInterceptors[[]*EntitlementGrant](ctx, egq, qr, egq.inters)
}

// AllX is like All, but panics if an error occurs.
func (egq *EntitlementGrantQuery) AllX(ctx context.Context) []*EntitlementGrant {
	nodes, err := egq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of EntitlementGrant IDs.
func (egq *EntitlementGrantQuery) IDs(ctx context.Context) (ids []string, err error) {
	if egq.ctx.Unique == nil && egq.path != nil {
		egq.Unique(true)
	}
	ctx = setContextOp(ctx, egq.ctx, ent.OpQueryIDs)
	if err = egq.Select(entitlementgrant.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (egq *EntitlementGrantQuery) IDsX(ctx context.Context) []string {
	ids, err := egq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (egq *EntitlementGrantQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, egq.ctx, ent.OpQueryCount)
	if err := egq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, egq, querierCount[*EntitlementGrantQuery](), egq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (egq *EntitlementGrantQuery) CountX(ctx context.Context) int {
	count, err := egq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (egq *EntitlementGrantQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, egq.ctx, ent.OpQueryExist)
	switch _, err := egq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (egq *EntitlementGrantQuery) ExistX(ctx context.Context) bool {
	exist, err := egq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the EntitlementGrantQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (egq *EntitlementGrantQuery) Clone() *EntitlementGrantQuery {
	if egq == nil {
		return nil
	}
	return &EntitlementGrantQuery{
		config:     egq.config,
		ctx:        egq.ctx.Clone(),
		order:      append([]entitlementgrant.OrderOption{}, egq.order...),
		inters:     append([]Interceptor{}, egq.inters...),
		predicates: append([]predicate.EntitlementGrant{}, egq.predicates...),
		// clone intermediate query.
		sql:  egq.sql.Clone(),
		path: egq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.EntitlementGrant.Query().
//		GroupBy(entitlementgrant.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (egq *EntitlementGrantQuery) GroupBy(field string, fields ...string) *EntitlementGrantGroupBy {
	egq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &EntitlementGrantGroupBy{build: egq}
	grbuild.flds = &egq.ctx.Fields
	grbuild.label = entitlementgrant.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.EntitlementGrant.Query().
//		Select(entitlementgrant.FieldTenantID).
//		Scan(ctx, &v)
func (egq *EntitlementGrantQuery) Select(fields ...string) *EntitlementGrantSelect {
	egq.ctx.Fields = append(egq.ctx.Fields, fields...)
	sbuild := &EntitlementGrantSelect{EntitlementGrantQuery: egq}
	sbuild.label = entitlementgrant.Label
	sbuild.flds, sbuild.scan = &egq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a EntitlementGrantSelect configured with the given aggregations.
func (egq *EntitlementGrantQuery) Aggregate(fns ...AggregateFunc) *EntitlementGrantSelect {
	return egq.Select().Aggregate(fns...)
}

func (egq *EntitlementGrantQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range egq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, egq); err != nil {
				return err
			}
		}
	}
	for _, f := range egq.ctx.Fields {
		if !entitlementgrant.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if egq.path != nil {
		prev, err := egq.path(ctx)
		if err != nil {
			return err
		}
		egq.sql = prev
	}
	return nil
}

func (egq *EntitlementGrantQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*EntitlementGrant, error) {
	var (
		nodes = []*EntitlementGrant{}
		_spec = egq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*EntitlementGrant).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &EntitlementGrant{config: egq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, egq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (egq *EntitlementGrantQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := egq.querySpec()
	_spec.Node.Columns = egq.ctx.Fields
	if len(egq.ctx.Fields) > 0 {
		_spec.Unique = egq.ctx.Unique != nil && *egq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, egq.driver, _spec)
}

func (egq *EntitlementGrantQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(entitlementgrant.Table, entitlementgrant.Columns, sqlgraph.NewFieldSpec(entitlementgrant.FieldID, field.TypeString))
	_spec.From = egq.sql
	if unique := egq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if egq.path != nil {
		_spec.Unique = true
	}
	if fields := egq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, entitlementgrant.FieldID)
		for i := range fields {
			if fields[i] != entitlementgrant.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := egq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := egq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := egq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := egq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (egq *EntitlementGrantQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(egq.driver.Dialect())
	t1 := builder.Table(entitlementgrant.Table)
	columns := egq.ctx.Fields
	if len(columns) == 0 {
		columns = entitlementgrant.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if egq.sql != nil {
		selector = egq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if egq.ctx.Unique != nil && *egq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range egq.predicates {
		p(selector)
	}
	for _, p := range egq.order {
		p(selector)
	}
	if offset := egq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := egq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// EntitlementGrantGroupBy is the group-by builder for EntitlementGrant entities.
type EntitlementGrantGroupBy struct {
	selector
	build *EntitlementGrantQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (eggb *EntitlementGrantGroupBy) Aggregate(fns ...AggregateFunc) *EntitlementGrantGroupBy {
	eggb.fns = append(eggb.fns, fns...)
	return eggb
}

// Scan applies the selector query and scans the result into the given value.
func (eggb *EntitlementGrantGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, eggb.build.ctx, ent.OpQueryGroupBy)
	if err := eggb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EntitlementGrantQuery, *EntitlementGrantGroupBy](ctx, eggb.build, eggb, eggb.build.inters, v)
}

func (eggb *EntitlementGrantGroupBy) sqlScan(ctx context.Context, root *EntitlementGrantQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(eggb.fns))
	for _, fn := range eggb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*eggb.flds)+len(eggb.fns))
		for _, f := range *eggb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*eggb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := eggb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// EntitlementGrantSelect is the builder for selecting fields of EntitlementGrant entities.
type EntitlementGrantSelect struct {
	*EntitlementGrantQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (egs *EntitlementGrantSelect) Aggregate(fns ...AggregateFunc) *EntitlementGrantSelect {
	egs.fns = append(egs.fns, fns...)
	return egs
}

// Scan applies the selector query and scans the result into the given value.
func (egs *EntitlementGrantSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, egs.ctx, ent.OpQuerySelect)
	if err := egs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*EntitlementGrantQuery, *EntitlementGrantSelect](ctx, egs.EntitlementGrantQuery, egs, egs.inters, v)
}

func (egs *EntitlementGrantSelect) sqlScan(ctx context.Context, root *EntitlementGrantQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(egs.fns))
	for _, fn := range egs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*egs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := egs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flexprice/flexprice/ent/custompricingunit"
	"github.com/flexprice/flexprice/ent/predicate"
	"github.com/flexprice/flexprice/ent/price"
)

// CustomPricingUnitQuery is the builder for querying CustomPricingUnit entities.
type CustomPricingUnitQuery struct {
	config
	ctx        *QueryContext
	order      []custompricingunit.OrderOption
	inters     []Interceptor
	predicates []predicate.CustomPricingUnit
	withPrices *PriceQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CustomPricingUnitQuery builder.
func (cpuq *CustomPricingUnitQuery) Where(ps ...predicate.CustomPricingUnit) *CustomPricingUnitQuery {
	cpuq.predicates = append(cpuq.predicates, ps...)
	return cpuq
}

// Limit the number of records to be returned by this query.
func (cpuq *CustomPricingUnitQuery) Limit(limit int) *CustomPricingUnitQuery {
	cpuq.ctx.Limit = &limit
	return cpuq
}

// Offset to start from.
func (cpuq *CustomPricingUnitQuery) Offset(offset int) *CustomPricingUnitQuery {
	cpuq.ctx.Offset = &offset
	return cpuq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (cpuq *CustomPricingUnitQuery) Unique(unique bool) *CustomPricingUnitQuery {
	cpuq.ctx.Unique = &unique
	return cpuq
}

// Order specifies how the records should be ordered.
func (cpuq *CustomPricingUnitQuery) Order(o ...custompricingunit.OrderOption) *CustomPricingUnitQuery {
	cpuq.order = append(cpuq.order, o...)
	return cpuq
}

// QueryPrices chains the current query on the "prices" edge.
func (cpuq *CustomPricingUnitQuery) QueryPrices() *PriceQuery {
	query := (&PriceClient{config: cpuq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := cpuq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := cpuq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(custompricingunit.Table, custompricingunit.FieldID, selector),
			sqlgraph.To(price.Table, price.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, true, custompricingunit.PricesTable, custompricingunit.PricesColumn),
		)
		fromU = sqlgraph.SetNeighbors(cpuq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CustomPricingUnit entity from the query.
// Returns a *NotFoundError when no CustomPricingUnit was found.
func (cpuq *CustomPricingUnitQuery) First(ctx context.Context) (*CustomPricingUnit, error) {
	nodes, err := cpuq.Limit(1).All(setContextOp(ctx, cpuq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{custompricingunit.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) FirstX(ctx context.Context) *CustomPricingUnit {
	node, err := cpuq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CustomPricingUnit ID from the query.
// Returns a *NotFoundError when no CustomPricingUnit ID was found.
func (cpuq *CustomPricingUnitQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cpuq.Limit(1).IDs(setContextOp(ctx, cpuq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{custompricingunit.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) FirstIDX(ctx context.Context) string {
	id, err := cpuq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CustomPricingUnit entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CustomPricingUnit entity is found.
// Returns a *NotFoundError when no CustomPricingUnit entities are found.
func (cpuq *CustomPricingUnitQuery) Only(ctx context.Context) (*CustomPricingUnit, error) {
	nodes, err := cpuq.Limit(2).All(setContextOp(ctx, cpuq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{custompricingunit.Label}
	default:
		return nil, &NotSingularError{custompricingunit.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) OnlyX(ctx context.Context) *CustomPricingUnit {
	node, err := cpuq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CustomPricingUnit ID in the query.
// Returns a *NotSingularError when more than one CustomPricingUnit ID is found.
// Returns a *NotFoundError when no entities are found.
func (cpuq *CustomPricingUnitQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = cpuq.Limit(2).IDs(setContextOp(ctx, cpuq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{custompricingunit.Label}
	default:
		err = &NotSingularError{custompricingunit.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) OnlyIDX(ctx context.Context) string {
	id, err := cpuq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CustomPricingUnits.
func (cpuq *CustomPricingUnitQuery) All(ctx context.Context) ([]*CustomPricingUnit, error) {
	ctx = setContextOp(ctx, cpuq.ctx, ent.OpQueryAll)
	if err := cpuq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CustomPricingUnit, *CustomPricingUnitQuery]()
	return withInterceptors[[]*CustomPricingUnit](ctx, cpuq, qr, cpuq.inters)
}

// AllX is like All, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) AllX(ctx context.Context) []*CustomPricingUnit {
	nodes, err := cpuq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CustomPricingUnit IDs.
func (cpuq *CustomPricingUnitQuery) IDs(ctx context.Context) (ids []string, err error) {
	if cpuq.ctx.Unique == nil && cpuq.path != nil {
		cpuq.Unique(true)
	}
	ctx = setContextOp(ctx, cpuq.ctx, ent.OpQueryIDs)
	if err = cpuq.Select(custompricingunit.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) IDsX(ctx context.Context) []string {
	ids, err := cpuq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (cpuq *CustomPricingUnitQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, cpuq.ctx, ent.OpQueryCount)
	if err := cpuq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, cpuq, querierCount[*CustomPricingUnitQuery](), cpuq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) CountX(ctx context.Context) int {
	count, err := cpuq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (cpuq *CustomPricingUnitQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, cpuq.ctx, ent.OpQueryExist)
	switch _, err := cpuq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (cpuq *CustomPricingUnitQuery) ExistX(ctx context.Context) bool {
	exist, err := cpuq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CustomPricingUnitQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (cpuq *CustomPricingUnitQuery) Clone() *CustomPricingUnitQuery {
	if cpuq == nil {
		return nil
	}
	return &CustomPricingUnitQuery{
		config:     cpuq.config,
		ctx:        cpuq.ctx.Clone(),
		order:      append([]custompricingunit.OrderOption{}, cpuq.order...),
		inters:     append([]Interceptor{}, cpuq.inters...),
		predicates: append([]predicate.CustomPricingUnit{}, cpuq.predicates...),
		withPrices: cpuq.withPrices.Clone(),
		// clone intermediate query.
		sql:  cpuq.sql.Clone(),
		path: cpuq.path,
	}
}

// WithPrices tells the query-builder to eager-load the nodes that are connected to
// the "prices" edge. The optional arguments are used to configure the query builder of the edge.
func (cpuq *CustomPricingUnitQuery) WithPrices(opts ...func(*PriceQuery)) *CustomPricingUnitQuery {
	query := (&PriceClient{config: cpuq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	cpuq.withPrices = query
	return cpuq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CustomPricingUnit.Query().
//		GroupBy(custompricingunit.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (cpuq *CustomPricingUnitQuery) GroupBy(field string, fields ...string) *CustomPricingUnitGroupBy {
	cpuq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CustomPricingUnitGroupBy{build: cpuq}
	grbuild.flds = &cpuq.ctx.Fields
	grbuild.label = custompricingunit.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.CustomPricingUnit.Query().
//		Select(custompricingunit.FieldTenantID).
//		Scan(ctx, &v)
func (cpuq *CustomPricingUnitQuery) Select(fields ...string) *CustomPricingUnitSelect {
	cpuq.ctx.Fields = append(cpuq.ctx.Fields, fields...)
	sbuild := &CustomPricingUnitSelect{CustomPricingUnitQuery: cpuq}
	sbuild.label = custompricingunit.Label
	sbuild.flds, sbuild.scan = &cpuq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CustomPricingUnitSelect configured with the given aggregations.
func (cpuq *CustomPricingUnitQuery) Aggregate(fns ...AggregateFunc) *CustomPricingUnitSelect {
	return cpuq.Select().Aggregate(fns...)
}

func (cpuq *CustomPricingUnitQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range cpuq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, cpuq); err != nil {
				return err
			}
		}
	}
	for _, f := range cpuq.ctx.Fields {
		if !custompricingunit.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if cpuq.path != nil {
		prev, err := cpuq.path(ctx)
		if err != nil {
			return err
		}
		cpuq.sql = prev
	}
	return nil
}

func (cpuq *CustomPricingUnitQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CustomPricingUnit, error) {
	var (
		nodes       = []*CustomPricingUnit{}
		_spec       = cpuq.querySpec()
		loadedTypes = [1]bool{
			cpuq.withPrices != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CustomPricingUnit).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CustomPricingUnit{config: cpuq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, cpuq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := cpuq.withPrices; query != nil {
		if err := cpuq.loadPrices(ctx, query, nodes,
			func(n *CustomPricingUnit) { n.Edges.Prices = []*Price{} },
			func(n *CustomPricingUnit, e *Price) { n.Edges.Prices = append(n.Edges.Prices, e) }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (cpuq *CustomPricingUnitQuery) loadPrices(ctx context.Context, query *PriceQuery, nodes []*CustomPricingUnit, init func(*CustomPricingUnit), assign func(*CustomPricingUnit, *Price)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[string]*CustomPricingUnit)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(price.FieldCustomPricingUnitID)
	}
	query.Where(predicate.Price(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(custompricingunit.PricesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.CustomPricingUnitID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "custom_pricing_unit_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (cpuq *CustomPricingUnitQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := cpuq.querySpec()
	_spec.Node.Columns = cpuq.ctx.Fields
	if len(cpuq.ctx.Fields) > 0 {
		_spec.Unique = cpuq.ctx.Unique != nil && *cpuq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, cpuq.driver, _spec)
}

func (cpuq *CustomPricingUnitQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(custompricingunit.Table, custompricingunit.Columns, sqlgraph.NewFieldSpec(custompricingunit.FieldID, field.TypeString))
	_spec.From = cpuq.sql
	if unique := cpuq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if cpuq.path != nil {
		_spec.Unique = true
	}
	if fields := cpuq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, custompricingunit.FieldID)
		for i := range fields {
			if fields[i] != custompricingunit.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := cpuq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := cpuq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := cpuq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := cpuq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (cpuq *CustomPricingUnitQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(cpuq.driver.Dialect())
	t1 := builder.Table(custompricingunit.Table)
	columns := cpuq.ctx.Fields
	if len(columns) == 0 {
		columns = custompricingunit.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if cpuq.sql != nil {
		selector = cpuq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if cpuq.ctx.Unique != nil && *cpuq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range cpuq.predicates {
		p(selector)
	}
	for _, p := range cpuq.order {
		p(selector)
	}
	if offset := cpuq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := cpuq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CustomPricingUnitGroupBy is the group-by builder for CustomPricingUnit entities.
type CustomPricingUnitGroupBy struct {
	selector
	build *CustomPricingUnitQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (cpugb *CustomPricingUnitGroupBy) Aggregate(fns ...AggregateFunc) *CustomPricingUnitGroupBy {
	cpugb.fns = append(cpugb.fns, fns...)
	return cpugb
}

// Scan applies the selector query and scans the result into the given value.
func (cpugb *CustomPricingUnitGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cpugb.build.ctx, ent.OpQueryGroupBy)
	if err := cpugb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CustomPricingUnitQuery, *CustomPricingUnitGroupBy](ctx, cpugb.build, cpugb, cpugb.build.inters, v)
}

func (cpugb *CustomPricingUnitGroupBy) sqlScan(ctx context.Context, root *CustomPricingUnitQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(cpugb.fns))
	for _, fn := range cpugb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*cpugb.flds)+len(cpugb.fns))
		for _, f := range *cpugb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*cpugb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cpugb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CustomPricingUnitSelect is the builder for selecting fields of CustomPricingUnit entities.
type CustomPricingUnitSelect struct {
	*CustomPricingUnitQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cpus *CustomPricingUnitSelect) Aggregate(fns ...AggregateFunc) *CustomPricingUnitSelect {
	cpus.fns = append(cpus.fns, fns...)
	return cpus
}

// Scan applies the selector query and scans the result into the given value.
func (cpus *CustomPricingUnitSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cpus.ctx, ent.OpQuerySelect)
	if err := cpus.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CustomPricingUnitQuery, *CustomPricingUnitSelect](ctx, cpus.CustomPricingUnitQuery, cpus, cpus.inters, v)
}

func (cpus *CustomPricingUnitSelect) sqlScan(ctx context.Context, root *CustomPricingUnitQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cpus.fns))
	for _, fn := range cpus.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cpus.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cpus.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

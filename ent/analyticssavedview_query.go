// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/flexprice/flexprice/ent/analyticssavedview"
	"github.com/flexprice/flexprice/ent/predicate"
)

// AnalyticsSavedViewQuery is the builder for querying AnalyticsSavedView entities.
type AnalyticsSavedViewQuery struct {
	config
	ctx        *QueryContext
	order      []analyticssavedview.OrderOption
	inters     []Interceptor
	predicates []predicate.AnalyticsSavedView
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AnalyticsSavedViewQuery builder.
func (asvq *AnalyticsSavedViewQuery) Where(ps ...predicate.AnalyticsSavedView) *AnalyticsSavedViewQuery {
	asvq.predicates = append(asvq.predicates, ps...)
	return asvq
}

// Limit the number of records to be returned by this query.
func (asvq *AnalyticsSavedViewQuery) Limit(limit int) *AnalyticsSavedViewQuery {
	asvq.ctx.Limit = &limit
	return asvq
}

// Offset to start from.
func (asvq *AnalyticsSavedViewQuery) Offset(offset int) *AnalyticsSavedViewQuery {
	asvq.ctx.Offset = &offset
	return asvq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (asvq *AnalyticsSavedViewQuery) Unique(unique bool) *AnalyticsSavedViewQuery {
	asvq.ctx.Unique = &unique
	return asvq
}

// Order specifies how the records should be ordered.
func (asvq *AnalyticsSavedViewQuery) Order(o ...analyticssavedview.OrderOption) *AnalyticsSavedViewQuery {
	asvq.order = append(asvq.order, o...)
	return asvq
}

// First returns the first AnalyticsSavedView entity from the query.
// Returns a *NotFoundError when no AnalyticsSavedView was found.
func (asvq *AnalyticsSavedViewQuery) First(ctx context.Context) (*AnalyticsSavedView, error) {
	nodes, err := asvq.Limit(1).All(setContextOp(ctx, asvq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{analyticssavedview.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) FirstX(ctx context.Context) *AnalyticsSavedView {
	node, err := asvq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AnalyticsSavedView ID from the query.
// Returns a *NotFoundError when no AnalyticsSavedView ID was found.
func (asvq *AnalyticsSavedViewQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = asvq.Limit(1).IDs(setContextOp(ctx, asvq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{analyticssavedview.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) FirstIDX(ctx context.Context) string {
	id, err := asvq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AnalyticsSavedView entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AnalyticsSavedView entity is found.
// Returns a *NotFoundError when no AnalyticsSavedView entities are found.
func (asvq *AnalyticsSavedViewQuery) Only(ctx context.Context) (*AnalyticsSavedView, error) {
	nodes, err := asvq.Limit(2).All(setContextOp(ctx, asvq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{analyticssavedview.Label}
	default:
		return nil, &NotSingularError{analyticssavedview.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) OnlyX(ctx context.Context) *AnalyticsSavedView {
	node, err := asvq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AnalyticsSavedView ID in the query.
// Returns a *NotSingularError when more than one AnalyticsSavedView ID is found.
// Returns a *NotFoundError when no entities are found.
func (asvq *AnalyticsSavedViewQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = asvq.Limit(2).IDs(setContextOp(ctx, asvq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{analyticssavedview.Label}
	default:
		err = &NotSingularError{analyticssavedview.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) OnlyIDX(ctx context.Context) string {
	id, err := asvq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AnalyticsSavedViews.
func (asvq *AnalyticsSavedViewQuery) All(ctx context.Context) ([]*AnalyticsSavedView, error) {
	ctx = setContextOp(ctx, asvq.ctx, ent.OpQueryAll)
	if err := asvq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AnalyticsSavedView, *AnalyticsSavedViewQuery]()
	return withInterceptors[[]*AnalyticsSavedView](ctx, asvq, qr, asvq.inters)
}

// AllX is like All, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) AllX(ctx context.Context) []*AnalyticsSavedView {
	nodes, err := asvq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AnalyticsSavedView IDs.
func (asvq *AnalyticsSavedViewQuery) IDs(ctx context.Context) (ids []string, err error) {
	if asvq.ctx.Unique == nil && asvq.path != nil {
		asvq.Unique(true)
	}
	ctx = setContextOp(ctx, asvq.ctx, ent.OpQueryIDs)
	if err = asvq.Select(analyticssavedview.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) IDsX(ctx context.Context) []string {
	ids, err := asvq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (asvq *AnalyticsSavedViewQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, asvq.ctx, ent.OpQueryCount)
	if err := asvq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, asvq, querierCount[*AnalyticsSavedViewQuery](), asvq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) CountX(ctx context.Context) int {
	count, err := asvq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (asvq *AnalyticsSavedViewQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, asvq.ctx, ent.OpQueryExist)
	switch _, err := asvq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (asvq *AnalyticsSavedViewQuery) ExistX(ctx context.Context) bool {
	exist, err := asvq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AnalyticsSavedViewQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (asvq *AnalyticsSavedViewQuery) Clone() *AnalyticsSavedViewQuery {
	if asvq == nil {
		return nil
	}
	return &AnalyticsSavedViewQuery{
		config:     asvq.config,
		ctx:        asvq.ctx.Clone(),
		order:      append([]analyticssavedview.OrderOption{}, asvq.order...),
		inters:     append([]Interceptor{}, asvq.inters...),
		predicates: append([]predicate.AnalyticsSavedView{}, asvq.predicates...),
		// clone intermediate query.
		sql:  asvq.sql.Clone(),
		path: asvq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AnalyticsSavedView.Query().
//		GroupBy(analyticssavedview.FieldTenantID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (asvq *AnalyticsSavedViewQuery) GroupBy(field string, fields ...string) *AnalyticsSavedViewGroupBy {
	asvq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AnalyticsSavedViewGroupBy{build: asvq}
	grbuild.flds = &asvq.ctx.Fields
	grbuild.label = analyticssavedview.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		TenantID string `json:"tenant_id,omitempty"`
//	}
//
//	client.AnalyticsSavedView.Query().
//		Select(analyticssavedview.FieldTenantID).
//		Scan(ctx, &v)
func (asvq *AnalyticsSavedViewQuery) Select(fields ...string) *AnalyticsSavedViewSelect {
	asvq.ctx.Fields = append(asvq.ctx.Fields, fields...)
	sbuild := &AnalyticsSavedViewSelect{AnalyticsSavedViewQuery: asvq}
	sbuild.label = analyticssavedview.Label
	sbuild.flds, sbuild.scan = &asvq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AnalyticsSavedViewSelect configured with the given aggregations.
func (asvq *AnalyticsSavedViewQuery) Aggregate(fns ...AggregateFunc) *AnalyticsSavedViewSelect {
	return asvq.Select().Aggregate(fns...)
}

func (asvq *AnalyticsSavedViewQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range asvq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, asvq); err != nil {
				return err
			}
		}
	}
	for _, f := range asvq.ctx.Fields {
		if !analyticssavedview.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if asvq.path != nil {
		prev, err := asvq.path(ctx)
		if err != nil {
			return err
		}
		asvq.sql = prev
	}
	return nil
}

func (asvq *AnalyticsSavedViewQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AnalyticsSavedView, error) {
	var (
		nodes = []*AnalyticsSavedView{}
		_spec = asvq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AnalyticsSavedView).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AnalyticsSavedView{config: asvq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, asvq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (asvq *AnalyticsSavedViewQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := asvq.querySpec()
	_spec.Node.Columns = asvq.ctx.Fields
	if len(asvq.ctx.Fields) > 0 {
		_spec.Unique = asvq.ctx.Unique != nil && *asvq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, asvq.driver, _spec)
}

func (asvq *AnalyticsSavedViewQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(analyticssavedview.Table, analyticssavedview.Columns, sqlgraph.NewFieldSpec(analyticssavedview.FieldID, field.TypeString))
	_spec.From = asvq.sql
	if unique := asvq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if asvq.path != nil {
		_spec.Unique = true
	}
	if fields := asvq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, analyticssavedview.FieldID)
		for i := range fields {
			if fields[i] != analyticssavedview.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := asvq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := asvq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := asvq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := asvq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (asvq *AnalyticsSavedViewQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(asvq.driver.Dialect())
	t1 := builder.Table(analyticssavedview.Table)
	columns := asvq.ctx.Fields
	if len(columns) == 0 {
		columns = analyticssavedview.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if asvq.sql != nil {
		selector = asvq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if asvq.ctx.Unique != nil && *asvq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range asvq.predicates {
		p(selector)
	}
	for _, p := range asvq.order {
		p(selector)
	}
	if offset := asvq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := asvq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AnalyticsSavedViewGroupBy is the group-by builder for AnalyticsSavedView entities.
type AnalyticsSavedViewGroupBy struct {
	selector
	build *AnalyticsSavedViewQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (asvgb *AnalyticsSavedViewGroupBy) Aggregate(fns ...AggregateFunc) *AnalyticsSavedViewGroupBy {
	asvgb.fns = append(asvgb.fns, fns...)
	return asvgb
}

// Scan applies the selector query and scans the result into the given value.
func (asvgb *AnalyticsSavedViewGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, asvgb.build.ctx, ent.OpQueryGroupBy)
	if err := asvgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnalyticsSavedViewQuery, *AnalyticsSavedViewGroupBy](ctx, asvgb.build, asvgb, asvgb.build.inters, v)
}

func (asvgb *AnalyticsSavedViewGroupBy) sqlScan(ctx context.Context, root *AnalyticsSavedViewQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(asvgb.fns))
	for _, fn := range asvgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*asvgb.flds)+len(asvgb.fns))
		for _, f := range *asvgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*asvgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := asvgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AnalyticsSavedViewSelect is the builder for selecting fields of AnalyticsSavedView entities.
type AnalyticsSavedViewSelect struct {
	*AnalyticsSavedViewQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (asvs *AnalyticsSavedViewSelect) Aggregate(fns ...AggregateFunc) *AnalyticsSavedViewSelect {
	asvs.fns = append(asvs.fns, fns...)
	return asvs
}

// Scan applies the selector query and scans the result into the given value.
func (asvs *AnalyticsSavedViewSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, asvs.ctx, ent.OpQuerySelect)
	if err := asvs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AnalyticsSavedViewQuery, *AnalyticsSavedViewSelect](ctx, asvs.AnalyticsSavedViewQuery, asvs, asvs.inters, v)
}

func (asvs *AnalyticsSavedViewSelect) sqlScan(ctx context.Context, root *AnalyticsSavedViewQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(asvs.fns))
	for _, fn := range asvs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*asvs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := asvs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
